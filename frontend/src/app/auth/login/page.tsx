import { LoginForm } from "@/components/auth/LoginForm";

export default function LoginPage(){
	return (
		<div>
			<h4 className="text-lg font-semibold text-center">The Moriah Project is currently in development mode - logging into an account is disabled.</h4>
			<LoginForm/>
		</div>
  	)
}